pipeline {


//for run on master
     // Define agent details here
    agent {label "master"}
    options {skipDefaultCheckout true}
    stages{
        stage('print job name'){
            steps{
                script{
                    bat 'set'
                    bat "python ..\\${FIRAT_JOB_NAME}\\print_job_name.py"
                }
            }
        }
        stage('run print the build number of the first job'){
            steps{
                script{
                    build job: 'print-first-job-number'
                }
            }
        }
    }
}